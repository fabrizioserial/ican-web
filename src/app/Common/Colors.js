export const primaryColor = '#000';
export const accentColor = '#6C63FF';
export const textColor = 'white';
export const inactiveColor = "#DDDDDD";

const colors = {
    primary: primaryColor,
    accent: accentColor,
    text: textColor,
    inactive: inactiveColor 
}

export default colors;